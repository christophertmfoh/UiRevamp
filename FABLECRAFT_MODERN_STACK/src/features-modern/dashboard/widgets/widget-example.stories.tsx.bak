import { Story } from '@ladle/react'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'

export default {
  title: 'Dashboard Widgets/Examples',
}

export const RecentProjectsWidget: Story = () => (
  <Card className="widget-w2 widget-h2">
    <CardHeader>
      <CardTitle>Recent Projects</CardTitle>
      <CardDescription>Your latest creative works</CardDescription>
    </CardHeader>
    <CardContent>
      <div className="space-y-2">
        {['Dragon\'s Quest', 'Space Odyssey', 'Mystery Manor'].map((project) => (
          <div key={project} className="flex items-center justify-between p-2 hover:bg-muted rounded">
            <span className="font-medium">{project}</span>
            <Button size="sm" variant="ghost">Open</Button>
          </div>
        ))}
      </div>
    </CardContent>
  </Card>
)

export const WritingGoalsWidget: Story = () => (
  <Card className="widget-w1 widget-h2">
    <CardHeader>
      <CardTitle>Writing Goals</CardTitle>
      <CardDescription>Daily progress</CardDescription>
    </CardHeader>
    <CardContent>
      <div className="space-y-4">
        <div>
          <div className="flex justify-between text-sm mb-1">
            <span>Today's Goal</span>
            <span>750/1000 words</span>
          </div>
          <div className="w-full bg-secondary rounded-full h-2">
            <div className="bg-primary h-2 rounded-full" style={{ width: '75%' }} />
          </div>
        </div>
        <div className="text-center">
          <div className="text-3xl font-bold text-primary">75%</div>
          <div className="text-sm text-muted-foreground">Keep going!</div>
        </div>
      </div>
    </CardContent>
  </Card>
)

export const TodoListWidget: Story = {
  args: {
    title: 'To-Do List',
    items: ['Outline Chapter 3', 'Research medieval weapons', 'Character backstory'],
    showCompleted: false,
  },
  render: (args) => (
    <Card className="widget-w1 widget-h3">
      <CardHeader>
        <CardTitle>{args.title}</CardTitle>
        <CardDescription>
          {args.items.length} items {args.showCompleted && '(showing completed)'}
        </CardDescription>
      </CardHeader>
      <CardContent>
        <div className="space-y-2">
          {args.items.map((item, index) => (
            <div key={index} className="flex items-center gap-2">
              <input type="checkbox" className="rounded" />
              <span>{item}</span>
            </div>
          ))}
          <Button className="w-full mt-4" variant="outline" size="sm">
            Add New Task
          </Button>
        </div>
      </CardContent>
    </Card>
  ),
}

export const WidgetSizes: Story = () => (
  <div className="grid grid-cols-4 gap-4 p-4 bg-muted/20">
    <Card className="widget-w1 widget-h1">
      <CardContent className="p-4">
        <div className="text-center">
          <div className="text-xs text-muted-foreground">1x1 Widget</div>
          <div className="text-2xl font-bold">42</div>
        </div>
      </CardContent>
    </Card>
    
    <Card className="widget-w2 widget-h1">
      <CardContent className="p-4">
        <div className="text-center">
          <div className="text-xs text-muted-foreground">2x1 Widget</div>
          <div className="text-2xl font-bold">Wide Widget</div>
        </div>
      </CardContent>
    </Card>
    
    <Card className="widget-w1 widget-h2 row-span-2">
      <CardContent className="p-4">
        <div className="text-center">
          <div className="text-xs text-muted-foreground">1x2 Widget</div>
          <div className="text-2xl font-bold mt-8">Tall</div>
        </div>
      </CardContent>
    </Card>
    
    <Card className="widget-w2 widget-h2 col-span-2 row-span-2">
      <CardContent className="p-4">
        <div className="text-center">
          <div className="text-xs text-muted-foreground">2x2 Widget</div>
          <div className="text-2xl font-bold mt-16">Large Square</div>
        </div>
      </CardContent>
    </Card>
  </div>
)